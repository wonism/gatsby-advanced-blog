import styled from 'styled-components';

export const Wrapper = styled.nav`
  margin: 1em 0;
  text-align: center;

  li {
    display: inline-block;
    padding: 0 .4em;
  }

  svg {
    margin: -2px 0 0;
  }
`;
